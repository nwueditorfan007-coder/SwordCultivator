Original prompt: 目前有个问题，我感觉射出的弹幕速度的延长线不是指向玩家这个圆心的，比如环阵下，速度的延长线都不指向圆形，你检查下所有阵型是否都有这个问题。以你专业的游戏设计能力来看，这个现象是在设计中的吗？

- Found two issues: source-slot selection was not paired with per-shot target direction in ring/fan style volleys, and ring preview spokes were hard-coded to 6 while the live slot count varies.
- Planned fix: choose each firing marble by best alignment to the current shot direction, use live slot count for ring firing angles, and align ring preview spokes with the absorbed marble count.
- Implemented the fix in `scripts/system/sword_array_controller.gd`, `scripts/system/main.gd`, and `scripts/system/game_renderer.gd`.
- Follow-up fix after screenshot review: `ring`, `fan`, and `crescent` now extend shots using the actual source bullet direction instead of an idealized pattern ray, removing residual off-center trajectories caused by discrete slot mismatch.
- Validation status: static code-path check only. No local `godot` / `godot4` executable was available in PATH, so runtime verification is still pending.
- 2026-03-28: Fixed the `146` boundary jump in `scripts/system/sword_array_controller.gd` by making `fan preconverge` fall back to the stable fan preview at zero progress and by reusing the same fan section profile as the starting envelope for `fan -> spear` convergence. Verified with a headless Godot load and a temporary numeric probe at distances `145 / 146 / 147 / 200 / 246 / 247`.
- 2026-03-28: Continued V2 by unifying `pierce`/late-`spear` launch origins. `scripts/system/sword_array_controller.gd` now exposes preview-aware fire launch anchors, and `scripts/system/main.gd` fires absorbed bullets from that anchor before computing velocity. Stable late-stage pierce now exits from the forward tip instead of individual side slots; fire effects were also moved forward to the same spear/pierce front. Verified with headless Godot load plus a temporary numeric probe at `247 / 320 / 420`.
- 2026-03-28: Refactored the sword-array firing pipeline for live geometry. `player.array_morph_state` now stays live while holding fire, while a new `array_fire_mode` keeps cadence/burst rules locked. This lets preview, remaining absorbed bullets, and fired-bullet guidance read the current morph geometry without reintroducing cadence jitter.
- 2026-03-28: Replaced the old `fan -> spear` shoulder-bonus envelope with staged profiles in `scripts/system/sword_array_controller.gd`: tightened fan sections, explicit arrow sections, then pierce sections with a late-stage monotonic narrowing blend. Added short-lived live guidance for fired bullets in `scripts/system/main.gd`, so bullets emitted during left-hold can keep adapting to current mouse distance before going fully ballistic. Verified with headless Godot load and a temporary probe showing live target drift (`260 -> 360`) plus narrowing widths across `247 / 280 / 320 / 360 / 398`.
- 2026-03-28: Further softened `fan -> closing arrow` by keeping flank curvature alive deeper into the arrow phase. `scripts/system/sword_array_controller.gd` now delays edge-curve release instead of linearly hardening the outline from the first blend frame.
- 2026-03-28: `scripts/system/game_renderer.gd` now smooths open profile outlines for section-based `fan/spear` previews with curve-strength-driven passes, so arc edges fade into straight arrow flanks gradually instead of snapping into visible折线. Verified with another headless Godot load.
- 2026-03-28: Revised the `fan -> pierce` middle state toward a `bulb spine` silhouette instead of an early hard arrow. `scripts/system/sword_array_controller.gd` now builds a bulb-like converging profile with separate `spine_focus` and `tip_focus`, while `scripts/system/game_renderer.gd` renders the center spine independently from the late tip highlight. Verified with another headless Godot load.
- 2026-03-28: Fixed the `ring_to_fan_end` boundary trapezoid. Section-based `fan` previews now keep curved inner/outer caps in `scripts/system/game_renderer.gd` instead of closing the band with straight edges, and cross-section guide lines fade in only after preconverge has meaningfully progressed. Verified with another headless Godot load.
- 2026-03-28: Added an explicit overlap window across `ring_to_fan_end`. `scripts/system/sword_array_controller.gd` now converts the late `crescent` into section geometry and blends it with early `fan preconverge` sections on both sides of the boundary, while `scripts/system/game_renderer.gd` uses true cap-control points for section fans so the boundary no longer switches renderer families in one frame. Verified with another headless Godot load.
- 2026-03-28: Reworked the preview chain around the user's “closed band” direction. `scripts/system/sword_array_controller.gd` now inserts a `continuous_band` section preview across the late `ring -> fan`, stable `fan`, and early `fan -> pierce` range, so `143`-ish and `273-340` no longer have to swap between `crescent` and open `spear` silhouette families. `scripts/system/game_renderer.gd` also lets section-based fan previews render spine/tip emphasis without losing the closed cap. Verified with another headless Godot load.
- 2026-03-28: Further tuned the closed-band path for two user-reported issues. `continuous_band` now starts earlier before `ring_to_fan_end`, reducing the `135`-range renderer family jump, and its front cap control is now pushed forward from the terminal section instead of sitting on the section center, so the middle closure reads as a convex bulb rather than an inward dent. Section-band previews also inherit blended transition colors through `preview_state`. Verified with another headless Godot load.
- 2026-03-28: Pushed the unified section-band pipeline further forward into the `ring -> fan` transition. `scripts/system/sword_array_controller.gd` now starts `continuous_band` shortly after `ring_stable_end`, and `_build_crescent_profile_sections()` no longer collapses near full-ring blends into degenerate arc samples; it first solves a controlled capsule-like opening band, then hands off to the true crescent sections as blend increases. This keeps early V2 preview/slot geometry in the same section language as the later fan/spear path instead of dropping back to the old polar crescent projection. Verified with a headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Follow-up fix for a regression introduced by that change: the earlier `continuous_band` handoff removed most of the visible `crescent/new-moon` phase and made the early `ring -> fan` silhouette read as an odd capsule. `scripts/system/sword_array_controller.gd` now restores the band handoff to the later overlap window (`ring_to_fan_end - 32`) and tones the crescent section regularization down to a light stabilizer, so the explicit crescent preview remains visible again through the main early transition. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Delayed the visible `ring -> fan` geometry onset without changing the global distance thresholds. `_get_ring_to_fan_preview()` in `scripts/system/sword_array_controller.gd` now remaps the raw transition blend through a delayed geometry window (`0.22 -> 0.94`) before shrinking the ring into the crescent/fan envelope, so the first part of the transition stays ring-like/new-moon-like longer instead of opening immediately after entering the morph zone. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Fixed another `crescent -> fan` mid-state regression after screenshot review. The unwanted “leaf/arched band” silhouette came from `continuous_band` using preconverged fan sections too early and from `game_renderer.gd` always forcing a minimum spine line on section-based fan previews. `scripts/system/sword_array_controller.gd` now blends the opening phase toward the stable fan section profile instead of the already-narrowing preconverge profile, and `scripts/system/game_renderer.gd` suppresses the forced center spine during `continuous_band / ring_fan_blend`. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Smoothed the `~127.9` jump at the `continuous_band` entry. `scripts/system/sword_array_controller.gd` now opens a short pre-entry overlap window (`12px` before `band_start`) and reuses the same section-band builder there, while keeping `entry_blend` clamped at zero until the real band start. That means the preview can switch onto the shared section pipeline before the visible new-moon -> fan opening begins, reducing the hard one-frame renderer family jump around the user-reported `127.9` distance. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Corrected a phase mix-up after the user referenced `docs/internal/fan_to_pierce_transition_sketch.svg`. That sketch describes the later `fan -> pierce` evolution, not the earlier `ring -> fan` evolution. `scripts/system/sword_array_controller.gd` now restores `ring -> fan` to a pure crescent path again by removing the delayed geometry remap / capsule regularization from `_get_ring_to_fan_preview()` and `_build_crescent_profile_sections()`, and by pushing `continuous_band` start back to `fan_stable_end - FAN_SPEAR_BLEND_BEFORE` so the shared band pipeline only re-enters during the late fan-to-pierce phase. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Investigated two new user-reported jumps and fixed them at their actual handoff points. The `141-144` jump came from `ring_fan_blend` starting too early (`ring_to_fan_end - 18`) and switching renderer family while blend was still near zero, so `RING_FAN_BLEND_BEFORE` is now reduced to `6` and `_get_ring_fan_blend_preview()` falls back to pure `crescent` until blend meaningfully rises. The `276-278` jump came from `continuous_band` entering late but immediately inheriting an already-advanced bulb blend and a stable-fan opening profile that did not match the previous late-fan preview; `_build_continuous_band_preview()` now starts from the current `fan_preconverge` geometry and clamps `bulb_start` to at least `band_start`, so the first continuous-band frame matches the outgoing fan before any further collapse begins. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Follow-up fix for two later user-reported jumps. The `157-160` jump was still caused by `ring_fan_blend` entering before the actual `ring_to_fan_end` boundary, so `RING_FAN_BLEND_BEFORE` is now `0` and the section-family overlap starts only after the threshold. The `316-319` jump came from `_build_continuous_band_preview()` asking `_get_fan_preconverge_progress()` for its opening profile; that helper returns `-1` after `fan_stable_end + FAN_SPEAR_BLEND_AFTER`, which previously collapsed the band entry fan profile from near-`1.0` back to `0.0` in one frame. The band builder now preserves `fan_progress = 1.0` after that point, so late-fan geometry stays continuous across the `319` boundary. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
- 2026-03-28: Added a short post-threshold plateau to remove the remaining `162-163` jump. `scripts/system/sword_array_controller.gd` now keeps the preview on pure stable fan geometry for `10px` after `ring_to_fan_end` (`RING_FAN_POST_THRESHOLD_PLATEAU`) before allowing either `ring_fan_blend` or `fan_preconverge` to start. This avoids the old behavior where crossing the `160.34` threshold immediately triggered another family/progress change a couple pixels later. Verified with another headless Godot run via `tools/start_godot_with_log.ps1 -Mode run -Headless -Wait -ExtraArgs '--quit-after','2'`.
